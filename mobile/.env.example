# Copy to .env.local for local preview. Values are safe to commit in CI/native builds
# (the anon key is public by design; row-level security protects data).
VITE_API_BASE=
VITE_SUPABASE_URL=https://<project-ref>.supabase.co
VITE_SUPABASE_ANON_KEY=<anon key from Supabase → Project Settings → API>
