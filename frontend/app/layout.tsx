import type { Metadata } from 'next'
import './globals.css'
import Header from './components/Header'
import Footer from './components/Footer'

export const metadata: Metadata = {
  title: 'IDMyPills - Identify Any Medication',
  description:
    'Identify any medication by imprint code, color, shape, or drug name. Free pill identification tool for patients and caregivers.',
  keywords: 'pill identifier, medication identification, imprint code, drug lookup',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className="bg-slate-50 text-slate-900 antialiased min-h-screen flex flex-col font-sans">
        <Header />
        <main className="flex-1">{children}</main>
        <Footer />
      </body>
    </html>
  )
}
