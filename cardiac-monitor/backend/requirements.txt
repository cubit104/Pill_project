# Core FastAPI dependencies
fastapi==0.104.1
uvicorn==0.24.0
starlette==0.27.0
pydantic==2.4.2
python-multipart==0.0.6

# Database dependencies
sqlalchemy==1.4.39
psycopg2-binary==2.9.9

# Data processing
pandas==2.1.3

# HTTP clients - pinned to stable versions with reliable wheel support
httpx==0.25.1
requests==2.31.0

# Security and utility libraries
cryptography==41.0.7
aiofiles==23.2.1
typing_extensions==4.8.0
python-dotenv==1.0.0

# Optional: aiohttp (only needed for specific async HTTP features)
# If you need aiohttp, use: aiohttp==3.8.6 (stable version with good wheel support)